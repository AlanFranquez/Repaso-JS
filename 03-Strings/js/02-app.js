const producto = 'Numero de variables';

console.log(producto.length);

console.log(producto.indexOf('variables'));  // Me dice en que posición se encuentra;

console.log(producto.includes('Numero'));