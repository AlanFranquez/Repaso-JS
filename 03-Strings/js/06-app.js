const nombre = 'Alan Fránquez';

console.log(nombre.repeat(3));

console.log(nombre.split(" ")) // Permite separar palabras segúyn un patrón por ejemplo spacios, lo hace una array

const hobbys = 'Comer, jugar a la pelota, entrenar, estudiar, mirar peliculas, leer';

console.log(hobbys.split(", "));

const prueba = prompt('Pon tus hobbys aquí');

console.log(prueba.split(", "))