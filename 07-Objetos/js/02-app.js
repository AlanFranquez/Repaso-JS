const auto = {
    nombre: 'Mustang',
    year: 1989,
    precio: 1700
}

console.log(auto);
// Para acceder a la propiedad de un objeto usamos el .

console.log(auto.nombre);
console.log(auto.year);
console.log(auto.precio);

// Tambien existe otra forma pero no es tan común

console.log(auto['nombre']);