const dias = ['Lunes', 'Martes', 'Miercoles', 'Jueves', 'Viernes', 'Sabado', 'Domingo'];

// Como agregar elementos a un array o modificarlo

dias[0] = 'LLLLLLLLLLLLL';
dias[7] = 'Día nuevo';

// Pregunta para obtener un trabajo, en que elementos se puede modificar una variable const? en un arreglo y en los objetos (si no están sellados)


console.log(dias)