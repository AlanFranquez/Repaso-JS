// Try Catch
// Lo que hace el trycatch es probar si un codigo anda, y si no es así entonces para la función
// Javascript por defecto para el codigo cuando hay un error, esto evita que eso pase

console.log(1+1);
try {
    hola();
} catch (error) {
    console.log(error)
}

console.log(2+2)